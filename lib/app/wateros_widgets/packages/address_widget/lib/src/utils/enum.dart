/// [LayoutMode] contain layout's style of widget

enum LayoutMode {
  oneColumn,
  twoColumn,
}

/// [WidgetMapping] is a mapping beetween widget in map
enum WidgetMapping {
  title,
  menu,
  labelStreet,
  labelTown,
  labelPostcode,
  labelCity,
  labelDistrict,
  labelWard,
  fieldStreet,
  fieldTown,
  fieldPostcode,
  fieldCity,
  fieldDistrict,
  fieldWard,
  example,
}
